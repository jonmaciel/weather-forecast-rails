# Weather Forecast

Ruby 4.0.6 and Rails 8.1.3.1. No database is required.

```sh
bin/setup --skip-server
bin/dev
```

Open http://localhost:3000. Run checks with `bin/rails test`,
`bin/rails zeitwerk:check` and `bin/rubocop`.
